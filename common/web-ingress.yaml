apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: studylinker-main
  namespace: webservice
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/group.name: studylinker
    alb.ingress.kubernetes.io/group.order: "4"

    # === ALB 전체에 적용되는 옵션들 ===
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP":80}]'
    alb.ingress.kubernetes.io/target-type: instance
spec:
  ingressClassName: alb
  rules:
    - host: gachon.studylink.click
      http:
        paths:
          # 👇 [추가됨] Auth 서비스 (10000번 포트)
          # 구체적인 경로를 먼저 적어주는 것이 관례상 좋습니다.
          - path: /api/auth
            pathType: Prefix
            backend:
              service:
                name: auth-service    # service.yaml의 metadata.name
                port:
                  number: 10000  
                       
          - path: /api/notifications
            pathType: Prefix
            backend:
              service:
                name: notification-service    # service.yaml의 metadata.name
                port:
                  number: 10000  

          - path: /api/recommend
            pathType: Prefix
            backend:
              service:
                name: recommend-service    # service.yaml의 metadata.name
                port:
                  number: 10000 

          - path: /api/user
            pathType: Prefix
            backend:
              service:
                name: user-service    
                port:
                  number: 10000 

          - path: /api/recommend
            pathType: Prefix
            backend:
              service:
                name: recommend-service    
                port:
                  number: 10000 

          - path: /api/stats
            pathType: Prefix
            backend:
              service:
                name: stats-service    
                port:
                  number: 10000

          - path: /api/system
            pathType: Prefix
            backend:
              service:
                name: system-service    
                port:
                  number: 10000  

          # 👇 [기존] 프론트엔드 웹 서비스 (80번 포트)
          - path: /
            pathType: Prefix
            backend:
              service:
                name: studylinker-web
                port:
                  number: 80
