apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-deployment
spec:
  replicas: 1           # HPA가 적용되면 이 숫자는 무시됩니다 (처음에만 유효)
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-container
        image: nugulsuhan/auth-service:7bb85fb902fa357f97a918a78cd4681d94a5f5c0
        
        # 👇 [중요] 이 부분이 없으면 HPA 절대 안 됩니다!
        resources:
          requests:
            cpu: "200m"     # 0.2 코어 (기준점)
            memory: "128Mi"
          limits:
            cpu: "500m"     # 0.5 코어 (최대치)
            memory: "256Mi"
